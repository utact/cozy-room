# 맵 컨셉 시스템 — 콘셉트 1 「코지 거실」 설계

> 작성일 2026-08-09, 갱신 2026-08-10. 대상: `잡아라! 코지 룸`
> 이 문서는 **콘셉트 1(코지 거실) 하나**의 설계다. 나머지 콘셉트는 각자 별도 스펙으로 다룬다.

---

## 1. 배경과 목표

현재 게임은 룸 테마 3종(거실·사무실·주방)이 있지만, 테마가 바꾸는 것은 바닥·벽 색과 가구 배치뿐이다.
프롭은 테마와 무관하게 **항상 전부** 스폰되고(`objects.ts:66`), 질문과 모드도 테마와 무관하게
랜덤으로 뽑힌다(`topics.ts`, `events.ts:246`). 그 결과 심야 주방에서 "층간소음 복수전" 질문이
나오고 스케이트보드를 집는 식의 어긋남이 생긴다.

목표는 **맵 컨셉을 질문·아이템·모드를 함께 묶는 단위로 만드는 것**이다. 한 콘셉트 안에서는
그 배경에 실제로 있을 법한 물건만 깔리고, 그 배경에서 납득되는 질문이 나오고,
그 배경의 사물이 일으킬 법한 모드만 발동한다.

### 콘셉트 로드맵

| # | 콘셉트 | 상태 |
| --- | --- | --- |
| 1 | 코지 거실 | **이 문서** — 게임 이름에 맞는 기본 테마 |
| 2 | 야근 사무실 | 후속 스펙 |
| 3 | 캠핑장 | 후속 스펙 |
| 4 | 편의점 | 후속 스펙 |

프롭은 콘셉트끼리 공유한다. 같은 집 안이라면 거실의 물건이 사무실에 있어도 어색하지 않다.
콘셉트마다 전용 세트를 새로 만들지 않고, 어울리는 것은 재사용해 전체 물량을 줄인다.

---

## 2. 선행 결정 사항

| 결정 | 내용 | 근거 |
| --- | --- | --- |
| **기존 자산 전면 폐기** | 현재의 프롭·맵·가구를 재사용하지 않는다 | 전부 임시 구현이었고 콘셉트에 맞춰 선정된 것이 아니다 |
| **절차적 폴백 제거** | GLB가 없을 때 프리미티브로 그리는 경로를 전부 삭제 | 폴백을 쓸 계획이 없다. 남겨두면 실제로 무엇이 렌더링되는지 불명확해진다 |
| **점프 없음** | 점프해야만 닿는 물건이 없어야 한다 | `feat/lobby-ui-renewal`에서 이미 제거됨(`input.ts:4`) |
| **은닉 없음** | 화면에서 가려지는 프롭이 존재해선 안 된다 | 탑뷰를 채택한 이유가 "물건 위치를 한눈에 파악"이다. 은닉은 그 목적과 충돌한다 |
| **최대 4인** | 1P·2P 키보드, 3P·4P는 AI 슬롯 | `game.ts:27` `MAX_PLAYERS = PLAYER_COLORS.length`(4색) |
| **캐릭터 유지** | `public/assets/characters/character.glb` 그대로 사용 | 교체 계획 없음 |

### 현재 자산 상태

- `public/assets/props/` — 프롭 GLB 없음. 현재 프롭은 전부 `catalog.ts`의 `parts[]`로 그린
  절차적 프리미티브다.
- `world.ts`의 방·가구 — 색깔 박스를 직접 그리는 코드뿐. **GLB 로딩 경로 자체가 없다.**
- `public/assets/characters/character.glb` — 존재. 유지.

프롭·가구·룸 셸은 모두 0에서 새로 만든다.

---

## 3. 아트 팔레트

`feat/lobby-ui-renewal`에서 UI가 명시적 팔레트를 갖게 됐다(`ui.ts:17-27`).

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--night` | `#191411` | 방의 그림자. **보랏빛 한 톨 없는 따뜻한 먹빛** |
| `--lamp` | `#f0a94c` | 유일한 강조색 — 스탠드 조명 |
| `--cream` | `#f7efe4` | 밝은 면. 순백 대신 따뜻한 종이색 |
| 플레이어 4색 | `#e4573d` `#3d7de4` `#e4b53d` `#4fbf5e` | 캐릭터 식별 전용 |

**3D 씬은 아직 이 팔레트를 따르지 않는다.** `world.ts:40`의 배경이 `0x241f38`, `themes.ts`의
거실 벽이 `0x5e5378`으로 둘 다 보라 계열이다. UI가 보라를 완전히 걷어냈으므로 3D 룸도 맞춰야 한다.

### 에셋 제작 규칙

- 프롭·가구·룸 셸은 **따뜻한 흙빛 계열**(테라코타·크림·앰버·웜 브라운)을 기준으로 한다.
- **보라·차가운 청회색을 쓰지 않는다.**
- 플레이어 4색은 캐릭터 식별용이므로 프롭의 주조색으로 쓰지 않는다. 물건과 사람이 헷갈린다.
- 씬 배경과 벽 색도 `--night` 계열로 교체한다.

---

## 4. 공간 설계

### 4.1 카메라가 정하는 제약

카메라는 `(0, 13.5, 12.5)`에서 `(0, 0, -0.5)`를 본다(`world.ts:43-45`). 부감 약 46°.
방은 `ROOM_W = 16`, `ROOM_D = 12`(`world.ts:7-8`).

높이 `h`인 가구가 뒤쪽(-z)으로 가리는 바닥 길이는 `h / tan(θ)`이고, 퍼스펙티브라 위치마다 다르다.

| 가구 위치 | 카메라 각도 | 가려지는 길이 |
| --- | --- | --- |
| 방 앞쪽 (z=+6) | 64° | 높이 × 0.48 |
| 방 중앙 (z=0) | 47° | 높이 × 0.93 |
| 뒷벽 (z=-6) | 36° | 높이 × 1.37 |

그림자는 **항상 -z 방향**으로 생긴다. 따라서 뒷벽에 밀착한 가구는 그림자가 벽 안쪽으로 떨어져
**바닥을 전혀 가리지 않는다.**

### 4.2 은닉 0 보장 규칙

1. **모든 가구를 벽면에 배치하고 방 중앙을 비운다.** 중앙에 장애물을 두면 그림자 배제 구역이
   한복판에 생겨 판독성을 해친다.
2. **프롭 스폰 위치에서 가구 발자국과 그 뒤 그림자 띠를 배제한다.** `objects.ts`의 `placeRandom`이
   공중에서 떨어뜨리는 방식이므로, 낙하 시작점이 아니라 **착지 지점**을 검증해야 한다.

### 4.3 잡기 리치가 정하는 가구 깊이

캡슐은 half-height 0.42 · 반지름 0.34(`visuals.ts:6-7`)라 전체 높이 1.52, 몸 중심은 0.76.
손은 몸 중심 앞 0.62 · 위 0.15(`player.ts:16`)이므로 높이 0.91. 잡기 반경은 1.15(`objects.ts:116`).

- 바닥 프롭(높이 0.15): 몸 중심에서 앞으로 **1.48**
- 가구 위 프롭(높이 0.5): 가구 가장자리에서 안쪽으로 **1.35**

**가구 깊이는 1.35 이하**여야 상판 안쪽 물건에 손이 닿는다. 아래 배치는 전부 1.0 이하다.

### 4.4 통로 폭

점프가 없으므로 허리 높이 가구는 넘을 수 없는 완전한 벽이다. 캡슐 지름 0.68 기준:

- 일반 통로 **1.2 이상**
- 압박용 좁은 길 **0.9** (상대와 몸싸움 나면 막힘)
- **0.7 이하는 통행 불가**

### 4.5 가구 배치

| 가구 | 배치 | 높이 | 깊이 | 역할 |
| --- | --- | --- | --- | --- |
| 소파 | 뒷벽 밀착 | 등받이 0.8 / 좌면 0.45 | 1.0 | 뒷벽이라 등받이가 높아도 바닥을 가리지 않는다 |
| 티테이블 | 소파 앞 0.9 | 0.4 | 0.9 | 그림자 0.37이 소파와의 틈에 떨어진다 → 그 띠는 스폰 배제 |
| TV장 | 앞벽 | 0.5 | 0.5 | 소파와 마주 본다 |
| 러그 | 중앙 | 0.02 | — | 그림자 없음. 시각적 구역 표시만 |

창문과 소파가 둘 다 뒷벽이므로, **소파를 창문 중앙에서 비켜 배치**해 바람의 출처가 가려지지 않게 한다.

### 4.6 난이도의 출처

은닉이 없으므로 "안 보여서 못 찾는" 압박은 설계에서 배제된다. 남는 것은 전부 **보이는데도 못 갖는** 종류다.

1. **판단 부하** — 프롭이 다 보이지만 이 질문에 뭐가 최선인지는 즉답이 안 된다.
   빗자루와 무선청소기 중 층간소음에 뭐가 나은지 고민하는 3초가 곧 손해다.
2. **유일성** — 종류마다 하나뿐이라 남이 먼저 집으면 차선책으로 내려가야 한다.
   **4인이면 후보 4개가 정확히 소진된다.**
3. **거리** — 정답이 방 반대편이면 보고도 늦는다.
4. **뺏기** — 던져 맞히기, 줄다리기.
5. **모드** — 은닉이 없는 대신 모드가 유일한 교란 장치가 된다. 정전은 셋 중 유일하게
   정체를 가리므로 가장 강한 카드다.

---

## 5. 라운드 구조와 질문

### 5.1 라운드 구조

라운드 수는 현행 3(`game.ts:23`)을 유지한다. **한 라운드 = 한 콘셉트(맵)**이다.
매 매치마다 보유 콘셉트 중 3개를 골라 라운드에 배정한다. **라운드가 바뀌면 맵도 바뀐다.**

3라운드의 구성은 **질문 라운드 2개 + 일치 라운드 1개**다(현행 `game.ts:224`와 동일하게
2라운드 이후 중 하나가 일치 라운드가 된다). 질문 라운드는 배정된 콘셉트의 질문 풀에서
1개를 랜덤으로 뽑는다.

콘셉트가 1개뿐인 현 시점에는 3라운드 모두 코지 거실이 되고, 질문 3개 중 2개가 쓰인다.

### 5.2 일치 라운드

3라운드 중 하나는 질문 대신 **"이 물건을 가져와라"**가 제시되고 모든 플레이어가 같은 물건 하나를
다툰다. AI 심사 없이 순수 쟁탈전으로 승부가 갈린다. 현행 `beginMatch` 구조를 유지한다.

**지목 물건은 6.3의 닮은꼴 그룹에 속한 프롭 중에서 뽑는다.** 같은 그룹의 미끼가 방에 함께
깔려 있으므로 급하게 집으면 낚인다. 그룹에 속하지 않은 프롭을 지목하면 낚일 여지가 없어
라운드가 단순한 달리기가 된다.

### 5.3 질문 3종

| 질문 | 태그 축 |
| --- | --- |
| 집에 불났다! 하나만 들고 튀어라! | 소중함 · 비쌈 |
| 층간소음 복수전, 무엇으로 응수할까? | 소음 · 무거움 |
| 넷플릭스 정주행 필수템은? | 포근함 · 전자기기 |

세 축이 서로 겹치지 않아, 어느 것이 뽑혀도 정답 후보군이 다르다. 한 매치에 2개만 쓰이므로
3개 풀로도 매치마다 조합이 달라진다.

**층간소음 질문은 진동 모드와 같은 세계관이다.** 둘이 같은 라운드에 겹치면 상황이 저절로 말이 된다.

---

## 6. 프롭 12종

| 프롭 | 태그 | 주 후보 질문 |
| --- | --- | --- |
| 가족 사진 액자 | 소중함 추억 납작함 | 화재 |
| 노트북 | 비쌈 전자기기 일 소중함 | 화재 |
| 여권·통장 파우치 | 소중함 안전 생존 | 화재 |
| 곰인형 | 귀여움 포근함 소중함 | 화재 |
| 빗자루 | 청소 길쭉함 소음 | 층간소음 |
| 무선청소기 | 청소 전자기기 무거움 소음 | 층간소음 |
| 블루투스 스피커 | 전자기기 파티 소음 | 층간소음 |
| 몬스테라 화분 | 인테리어 식물 무거움 | 층간소음 |
| 극세사 담요 | 포근함 수면 부드러움 | 넷플릭스 |
| 쿠션 | 포근함 부드러움 인테리어 | 넷플릭스 |
| TV 리모컨 | 전자기기 일상 납작함 | 넷플릭스 |
| 헤드폰 | 전자기기 수면 소중함 | 넷플릭스 |

질문마다 후보가 정확히 4개다. **플레이어 정원과 같으므로 4인 매치에서는 후보가 남김없이 소진된다.**
넷이 다 후보를 쥐어도 점수는 갈린다 — 최선 하나와 차선 셋의 적합도가 다르기 때문이다.
나머지 8개는 그 라운드의 오답 후보로 남아 판단 부하를 만든다.

### 6.1 제작 제약

- 크기 **0.2~0.7** 범위.
- **위에서 내려다본 실루엣으로 구분되어야 한다.** 정전 모드가 실루엣만 보여주고, 평상시에도
  46° 부감이라 물건의 옆모습을 볼 일이 거의 없다. 옆에서 봐야 알아볼 수 있는 형태는 실패다.
- **뭉툭한 게임 프롭 비율**로 만든다. 가는 줄기·얇은 손잡이처럼 부러질 듯한 부분이 없어야 한다.
  탑뷰에서 사라지고, 물리 프롭으로 던져질 때 콜라이더와 어긋난다.
- 3절의 팔레트를 따른다.
- **단, 6.3의 닮은꼴 그룹은 실루엣 구분의 의도적 예외**다. 그룹 안에서는 반대로 서로 닮아야 한다.

### 6.2 생성 방법

프롭의 형태에 따라 두 가지 경로를 쓴다.

| 경로 | 대상 | 방법 |
| --- | --- | --- |
| **이미지 경유** | 유기적이거나 가늘어지기 쉬운 형태 — 곰인형 · 극세사 담요 · 빗자루 · 무선청소기 | 2D 레퍼런스 이미지를 먼저 만들어 비율과 실루엣을 확정한 뒤 3D로 변환한다 |
| **텍스트 직행** | 박스형이라 비율이 어긋날 여지가 적은 형태 — 액자 · 노트북 · 파우치 · 스피커 · 쿠션 · 리모컨 · 헤드폰 | 텍스트에서 바로 3D를 생성하고 텍스처를 입힌다 |

이미지 경유를 쓰는 이유는, **텍스트 직행이 유기적 형태에서 실물 비율로 흘러가기 때문**이다.
몬스테라 화분을 텍스트로 두 번 시도했을 때 모두 줄기가 가늘고 잎이 흩어져 탑뷰 실루엣이 성립하지
않았고, 같은 프롬프트를 2D 이미지로 먼저 확정한 뒤 3D로 옮기자 한 번에 뭉툭한 비율이 나왔다.
2D 단계에서 형태를 눈으로 확인하고 고칠 수 있다는 것이 이 경로의 이점이다.

### 6.3 닮은꼴 그룹

위에서 봤을 때 실루엣이 서로 닮은 프롭 묶음이다. **정전 모드에서 판단 부하를 만드는 핵심 장치**이며,
일치 라운드의 낚임도 여기서 나온다. `catalog.ts`의 `LOOKALIKES`에 그룹을 정의한다.

| 그룹 | 프롭 | 위에서 본 공통 형태 | 오답 파괴력 |
| --- | --- | --- | --- |
| A | 몬스테라 화분 · 블루투스 스피커 | 둥근 덩어리 | 높음 — 넷플릭스에 화분, 층간소음에 화분을 집으면 갈린다 |
| B | 가족 사진 액자 · 노트북 · TV 리모컨 | 납작한 사각형 | 높음 — 화재에서는 액자·노트북이 정답이지만 넷플릭스에서는 리모컨만 정답 |
| C | 극세사 담요 · 쿠션 | 부드러운 사각 덩어리 | 낮음 — 태그가 겹쳐 잘못 집어도 손해가 작다 |

**제작 제약**: 같은 그룹 안의 프롭은 **최대 변 길이를 서로 ±20% 안에 맞춘다.** 크기가 크게 다르면
실루엣만으로도 구별돼 그룹이 무의미해진다.

---

## 7. 모드 3종

모드는 그냥 발동하는 것이 아니라 **방 안에 있는 장치가 원인이어야 한다.** 각 모드는 대응하는
고정 오브젝트를 가지며, 그 오브젝트는 모드가 꺼져 있을 때도 방에 보인다.

### 7.1 돌풍 — "누가 또 창문 안 닫았어!"

**장치**: 뒷벽 중앙의 창문. 폭 약 3, 벽 중간 높이. 평소에도 살짝 열려 있고 커튼이 미세하게 흔들려
"저거 언젠가 터지겠다"는 복선을 상시 깔아둔다.

- 바람 방향은 창문 위치에서 나오므로 **항상 고정** — 뒷벽에서 앞벽으로(+z), 화면상 위에서 아래로.
  현행 `WindEvent`가 매번 랜덤 각도를 뽑는 로직(`events.ts:102-103`)은 삭제한다.
- **바람 존**: 방 전체가 아니라 **창문 x중심 기준 폭 약 5인 띠**(깊이는 방 전체)에만 힘이 작용한다.
  존 밖은 완전한 무풍.
- **가로지르기**: 존을 좌우로 통과하는 플레이어에게 존 안에 있는 동안 +z 임펄스가 누적되어
  이동 경로가 하류 쪽으로 휜다. 똑바로 가려 해도 목적지보다 앞으로 밀린다. 존을 벗어나면 즉시 복귀.
- 존 안의 프롭에도 같은 힘과 굴림 토크가 작용한다.
- 잠잠(2~3초) / 강풍(2.6초) 교대. **잠잠 구간에도 커튼은 계속 흔들려** 창문이 열려 있다는 신호를
  끊지 않는다. 스트릭 파티클만 강풍 때 표시한다.

### 7.2 진동 — "윗집에서 또 쿵쿵거려!"

**장치**: 천장 펜던트 조명. 쿵 할 때마다 흔들린다.

- 이벤트 동안 중력을 `-9.81 → -6.5`로 완화한다. 현행 달 중력의 `-2.6`(`events.ts:46`)은
  과하게 둥둥 떠서 "묵직한데 살짝 가벼워진" 체감과 맞지 않는다.
- **1.3~1.6초의 불규칙한 간격**으로 "쿵" 임팩트가 발생한다. 매 임팩트마다:
  - 짧고 굵은 카메라 쉐이크(0.15초)
  - 펜던트 조명 흔들림
  - 바닥 프롭에 수직 임펄스 → 위치가 미세하게 흐트러진다
  - 플레이어도 같은 임펄스를 받아 들썩인다. "붕 뜨는" 체감은 이 순간의 임팩트에서 나온다
  - 저음 "쿵" 효과음

### 7.3 정전 — "정전이야! 물건이 실루엣만 보여!"

**장치**: 돌풍과 **같은 창문**을 재사용한다. 정전 중에는 메인 조명이 꺼지고 그 창문으로만
바깥 가로등의 희미한 빛이 들어온다. 두 모드가 같은 오브젝트를 공유해 공간에 일관성이 생긴다.

- 메인 조명을 크게 낮추고 창문발 보조광만 남긴다.
- 각 플레이어를 따라다니는 미약한 개인 광원을 추가한다. 현행 코드에 없는 신규 구현이다.
- 프롭은 실루엣으로 바뀌고, **직접 잡은 것만 정체가 공개**된다(현행 `MysteryEvent` 로직 유지).
- 정전 시작 "뚝" 노이즈 + 웅웅거리던 소음이 끊기는 효과음.

### 7.4 제거되는 모드

| 대상 | 위치 | 사유 |
| --- | --- | --- |
| 바닥 왁스칠 | `events.ts:27` | 거실에서 바닥이 미끄러워질 서사적 장치가 없다 |
| 사물들이 이상해졌다(추상화) | `events.ts:206` | 정전과 역할이 겹친다. 정전이 서사적으로 우월하다 |

`visuals.ts`의 `buildAbstractVisual`도 추상화 모드와 함께 삭제한다.

### 7.5 폭격 비행선 — 콘셉트별 on/off

`airship.ts`는 **삭제하지 않는다.** 다른 콘셉트에서 쓸 여지가 있으므로 코드는 남기고,
콘셉트 정의에 on/off 플래그를 두어 제어한다.

**코지 거실은 off.** 거실 천장 위로 폭격 비행선이 지나갈 근거가 없고, "모드는 스토리에 맞는
장치를 가져야 한다"는 원칙에 어긋난다.

---

## 8. 에셋 목록

### 8.1 프롭 GLB — 12종

6절의 표 참조. 생성 경로는 6.2를 따른다.

### 8.2 가구 GLB — 3종

소파 · 티테이블 · TV장. 치수는 4.5절 참조.

### 8.3 모드 장치 GLB — 2종

| 장치 | 위치 | 연결 모드 | 요구사항 |
| --- | --- | --- | --- |
| 창문 + 커튼 | 뒷벽 중앙, 살짝 열림 | 돌풍(바람 출처) · 정전(유일 광원) | **커튼이 몸체와 분리된 메쉬**여야 한다 — 평상시 미세하게, 돌풍 때 크게 펄럭인다. 이미지 경유로 만든다 |
| 펜던트 조명 | 천장 중앙 | 진동 | 매달린 구조 — 쿵 할 때마다 흔들린다 |

### 8.4 룸 셸 텍스처 — 3종

바닥재(원목 마루) · 벽지 · 러그 무늬. 3절의 팔레트를 따른다.

### 8.5 에셋이 아닌 것

아래는 전부 코드로 처리되며 생성할 파일이 없다.

- 바람 스트릭 파티클, 정전 시 개인 광원, 카메라 쉐이크 — 코드 연출
- 효과음(쿵 · 바람 · 정전 뚝) — WebAudio 합성(`sound.ts`)
- 캐릭터 — 기존 `character.glb` 유지

---

## 9. 코드 영향 범위

### 9.1 폴백 제거

| 위치 | 제거 대상 |
| --- | --- |
| `catalog.ts` | `PropMeta.parts` 및 모든 프롭의 `parts[]` 정의 |
| `assets.ts:31-38` | 매니페스트가 없으면 조용히 절차적 비주얼로 넘어가는 경로. 로드 실패는 에러로 |
| `character.ts:191` | GLB가 없을 때의 절차적 캡슐 |
| `world.ts:buildRoom` | 색깔 박스로 방·가구를 그리는 코드 |

### 9.2 신규 구현

- **가구 GLB 로딩 경로** — `world.ts`에 존재하지 않는다. 프롭용 `AssetLibrary`와 같은 방식으로
  새로 만든다. 가구는 시각 메쉬(GLB)와 물리 콜라이더(수동 박스)를 분리해 갖는다.
- **스폰 배제 구역** — `objects.ts`의 `placeRandom`에 가구 발자국 + 그림자 띠 검증을 추가.
  낙하 시작점이 아니라 착지 지점 기준.
- **정전 개인 광원** — 플레이어를 따라다니는 PointLight.
- **진동 임팩트** — 카메라 쉐이크, 장치 흔들림, 임펄스.

### 9.3 기존 코드 수정

| 위치 | 변경 |
| --- | --- |
| `topics.ts` | 질문을 5.3의 3개로 교체 |
| `events.ts` | 모드 5종 → 3종. 돌풍의 랜덤 방향을 창문 기준 고정 방향 + 존 제한으로 교체 |
| `themes.ts` | 색·가구 배열 구조를, 질문·프롭·모드·비행선 on/off를 함께 묶는 콘셉트 단위로 재설계 |
| `catalog.ts` | 프롭을 6절의 12종으로 교체. `LOOKALIKES`를 6.3의 3개 그룹으로 교체 |
| `game.ts` | 라운드별 콘셉트 배정 로직 추가. `beginMatch`의 지목 대상을 닮은꼴 그룹 소속 프롭으로 제한. 비행선 호출을 콘셉트 플래그로 감쌈 |
| `world.ts:40` | 씬 배경·안개 색을 `--night` 계열로 교체 (현재 `0x241f38` 보라) |
| `visuals.ts` | `buildAbstractVisual` 삭제 |
| `sound.ts:70` | `jump()` 삭제 — 점프가 없어졌는데 남아 있다 |
| `bot.ts:6` | "테이블 위면 점프" 주석 정리 |
| `docs/1-게임소개.md` | 점프·구 모드 목록·라운드 구조·인원 갱신 |

### 9.4 콜라이더

**GLB에서 자동 추출하지 않고, 카탈로그에 프리미티브를 손으로 적는다.** 자동 convex hull은
액자·리모컨 같은 얇은 물건에서 부정확해지고 잡기·던지기 체감이 예측 불가능해진다. 12개는
수동 튜닝이 감당되는 규모다.

`catalog.ts`의 `PropMeta`에서 `parts[]`를 삭제하면 남는 필드는 `id / name / tags / density /
shape / size`이고, `shape`·`size`는 콜라이더 전용이 된다.

---

## 10. 검증 기준

1. 어떤 라운드에서도 **가구에 가려 보이지 않는 프롭이 없다.** 12개 전부가 화면에서 식별 가능하다.
2. 가구 상판에 얹힌 프롭이 **전부 잡힌다.** 손이 닿지 않는 위치에 놓이는 프롭이 없다.
3. 점프 입력이 존재하지 않으며, **점프 없이 12개 프롭 전부에 접근 가능하다.**
4. GLB가 하나라도 없으면 **절차적 도형이 아니라 에러**가 난다.
5. 돌풍의 바람이 **항상 창문에서 시작해 같은 방향으로** 흐르고, 존 밖에서는 아무 힘도 받지 않는다.
6. 정전 중 프롭은 실루엣이고, 잡은 것만 정체가 공개된다.
7. 3개 질문 **각각에 후보 프롭이 4개** 존재한다. 4인 매치에서 정확히 소진된다.
8. 정전 중 같은 닮은꼴 그룹의 프롭은 **실루엣만으로 구별되지 않는다.** 그룹 밖 프롭은 구별된다.
9. 일치 라운드의 지목 물건은 **항상 닮은꼴 그룹 소속**이며, 같은 그룹의 미끼가 방에 함께 깔린다.
10. 코지 거실 라운드에서 **폭격 비행선이 등장하지 않는다.**
11. 프롭·가구·룸 셸에 **보라 계열 색이 없다.** UI 팔레트와 3D 씬이 같은 색 체계를 쓴다.
